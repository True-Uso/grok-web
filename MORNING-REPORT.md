# Super Coding 早上报告

测试时间：2026-09-20 23:40 左右（你休息期间）  
账号：配置预置 `admin` / `coding123`  
模型：deepseek-flash  
入口：先打开主页 `http://127.0.0.1:8787/`，再进工作台 `/app`

---

## 1. 主页

打开网站先看到设计主页，不再一进来就是工作台。

- 桌面（约 1920 宽）：标题、按钮、三种模式、工厂三层、能力、步骤都在。脚本扫过标题/正文/按钮，**没有文字被横向挤出**。
- 工作台示意图是 CSS 画的，**没有用外部图片**，所以没有裂图。
- 手机 390 宽：导航收成「登录 / 开始做」；右侧大示意图换成手机版工作台一角；**同样没有文字溢出**。
- 「登录」弹出账号密码框。预置账号能进去。
- 「开始做」未登录会先登录，登录后进 `/app`。

预置账号写在 `.env`（没有的话用默认值）：

- `SC_BOOTSTRAP_USER=admin`
- `SC_BOOTSTRAP_PASS=coding123`

---

## 2. 可用性抽测

### 小任务：单文件问候页

需求：只要一个 html，标题「你好」加一行欢迎，立刻停。

- **发出到完成：约 15–20 秒**
- 结果：生成 `hello.html`，预览栏自动打开，工作台显示「已完成 · 1 步」

### 大任务：学生登记系统

需求：能录入姓名/班级、能查列表；Node.js + SQLite + Dockerfile；监听 8080。

- **发出到预览里出现可打开的应用：约 6 分钟**
- **发出到对话完全收口（发送键恢复）：约 8 分钟（约 21 步）**
- 结果：写了 `package.json`、`server.mjs`、`public/index.html`、`Dockerfile`；容器预览 `127.0.0.1:54901` 返回 HTTP 200，页面标题是学生登记系统。

大任务里模型仍会多跑几轮检查命令，所以「能预览」比「它自己说做完」更早。

---

## 3. 怎么记这两个数字

| 类型 | 一般耗时（这次实测） | 说明 |
| --- | --- | --- |
| 小任务（一个 html 小页面） | **大约半分钟以内** | 这次 15–20 秒 |
| 大任务（带后端、数据库、容器） | **大约 6–8 分钟** | 约 6 分钟能预览，约 8 分钟对话收口 |

这是 `deepseek-flash`、本机 Docker 可用时的一次样本，不是平均实验室数据。网络和 Docker 第一次拉镜像还会更久。

---

## 4. 还看到的问题

- 大任务结束时的中文回复偏短（「已经处理好了」），没有按产品要求把默认用法讲清楚，但应用本身已经能打开。
- 工作台里个别步骤名会带上本地路径，用户不一定需要看到。
